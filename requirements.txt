# Phase 1 — Scraper dependencies
# Install with: pip install -r requirements.txt

feedparser==6.0.11      # RSS/Atom feed parsing (Twitter via Nitter, newsletters)
httpx==0.27.0           # Modern HTTP client (Reddit JSON API)
