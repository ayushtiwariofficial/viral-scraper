# Viral Scraper — Python dependencies
# Install: pip install -r requirements.txt

feedparser==6.0.11      # RSS parsing (Reddit, newsletters, Twitter/Nitter)
httpx==0.27.0           # HTTP client (Groq, Gemini, LinkedIn API calls)
supabase==2.10.0        # Postgres database client
