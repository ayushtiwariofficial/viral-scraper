# Viral Scraper — Python dependencies
# Install: pip install -r requirements.txt
# Then:    python -m playwright install chromium

feedparser==6.0.11      # RSS parsing (Reddit, newsletters, Twitter/Nitter)
httpx==0.27.0           # HTTP client (Groq API, Gemini API calls)
playwright==1.61.0      # Browser automation for LinkedIn posting
supabase==2.10.0        # Postgres database client (replaces SQLite + artifacts)
